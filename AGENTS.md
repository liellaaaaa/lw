# AGENTS.md — 知识库操作宪法（AI 管理员规范）

> 本文件是任何 AI 助手（Claude Code / CodeBuddy 等）进入本知识库时的**唯一权威入口与行为宪法**。
> **所有智能体必须先阅读此处**，再执行任何操作。宪法随体系发展定期更新；重大调整需与主人（肖聪）确认。

## 0. 主人画像（写进每条指令的上下文）

- **身份**：肖聪，AI 技术专员（早期笔记曾用"总经办助理"，旧内容统一替换为 AI 技术专员）。
- **工作域 A · 宏昊AI助手**：供应商提供的企业级 AI 平台，模块含 培训助手 / 视频生成助手 / 说明书助手 / 销售 SOP 助手 / 简历助手 / 权限模块 / 获客系统 / 文档管理中心（用户统称"各 xx 助手"）。
- **工作域 B · ai-sales-coach**：自有项目——销售陪练 Web 应用（仓库 `github.com/liellaaaaa/ai-sales-coach`），**2026 年 7 月中旬才接手**，不要描述成从 4 月起的悠久个人历史。
- **时间粒度**：只到年月日（如 `2026-07-29`），不写时分。
- **性格注记（DISC · S+C 型）**：知识动机强、要"成为专家"；在**清晰流程 + 稳定节奏**下最出活；反感随性/模糊标准。→ 本知识库的"固定流程跑通一份材料"正是为其量身；AI 给出的步骤必须具体、可执行，不要留模糊空间。

> ⚠️ 易混淆红线：宏昊AI助手（供应商平台）≠ ai-sales-coach（自有项目）。4–6 月周报/日报里的"培训助手"等一律指宏昊AI助手，不得链到或写成 ai-sales-coach 的历史。

## 1. 核心架构原则

- **文件夹 = 流水线阶段（自生长）**，不是领域分类。资料按"进来→消化→方法→输出→复盘"单向流动。
- **标签 = 属性**（状态/优先级/类型/领域），不用于表达领域。格式 `#一级标签/二级标签`。
- **双链 = 知识网络**。用 `[[准确的笔记标题]]` 连接，让笔记互相引用、避免孤岛。
- 一句话原则：**收藏只是入口，回流才是知识库开始自生长的地方。**

## 2. 目录结构与权限边界

| 文件夹 | 用途 | AI 权限 |
|--------|------|---------|
| `00-原始资料/` | 收件箱：所有待处理原始材料（文章/逐字稿/课程笔记/读书摘录/聊天记录/灵感/网页摘录/旧库遗留） | **只读 + 可移动分类，但绝不删除**（宪法铁律） |
| `01-AI消化笔记/` | AI 产出的消化笔记（原子化，必答"对我有什么用"） | **可读写** |
| `02-方法库与提示词/` | 消化提示词、模板、可复用方法 | **默认只读**（用户主导；除非用户明确指令才改） |
| `03-输出成果/` | 文章/知乎回答/小红书/视频脚本/学习总结 + 输出回流卡 | **可读写** |
| `04-复盘/` | 周复盘 + 每周知识网络体检报告 | **可读写** |

> 只读目录 AI 只有读取权，除非用户明确指令，不得修改其中的任何文件。

## 3. 标签规范

- 状态：`#status/待处理` `#status/进行中` `#status/已完成`
- 优先级：`#priority/高` `#priority/中` `#priority/低`
- 类型：`#type/教程` `#type/笔记` `#type/想法` `#type/素材` `#type/原子`
- 领域：`#domain/宏昊AI助手` `#domain/ai-sales-coach` `#domain/销售` `#domain/管理` `#domain/AI技术`
- 规则：每篇经 AI 处理的笔记加 2–3 个精准标签；标签层级保持简洁，不过度细分。

## 4. AI 五大职责

**职责一：消化 Inbox（00 → 01）**
- 处理 `00-原始资料/` 中带 `#status/待处理` 或用户指定材料。
- 提炼核心，重写为**原子化笔记**（一篇只讲一个概念/事实）。
- 必答"这份材料对我有什么用"，按 `02-方法库与提示词/原始资料消化提示词.md` 的结构输出。
- 为笔记加 2–3 个标签，并建 ≥3 个 `[[相关笔记]]` 双链。

**职责二：维护知识网络（MOC）**
- 任一主题（子文件夹）笔记数 ≥ 5 篇时，自动创建/更新 `MOC - 主题名.md`。
- MOC 含：核心理论列表（带简介）、应用场景、相关主题链接。

**职责三：强制格式规范**
- 严格套用 `02-方法库与提示词/模板/` 中的对应模板。
- 内部链接一律 `[[准确标题]]`；标签用 `#一级/二级` 层级格式。
- 读书笔记固定三段：`个人收获 / 过往错点 / 今后改进`（不得改成"反思/"前缀）；读书笔记不反向链周报/日报，末尾只留"同系列 / 个人档案 / 总览"。

**职责四：每周知识网络体检（进 04-复盘/）**
- 每周输出报告：① 孤立笔记清单（无入链/出链）② 核心枢纽节点（被链最多）③ 标签使用统计与优化建议 ④ 知识缺口（推测缺内容的关联领域）。

**职责五：支持知识输出（→ 03-输出成果/）**
- 按 `02-方法库与提示词/选题与输出提示词.md` 整合内容。
- 所有输出必须附**输出回流卡**；默认先文章化，再视频化。

## 5. 工作流（单份材料的自生长闭环）

1. **收集** → 丢进 `00-原始资料/`（原始资料不用先整理，先放进来）
2. **消化** → AI 生成 `01-AI消化笔记/`（原子化 + 答"对我有什么用" + 标签 + 双链）
3. **方法沉淀** → 可复用套路进 `02-方法库与提示词/`
4. **输出** → 产出 `03-输出成果/`（附回流卡）
5. **复盘** → 记 `04-复盘/`，每周体检
6. **主题成网** → 某主题 ≥5 篇时建 `MOC - 主题名.md`

## 6. 铁律（不可违背）

1. 进入必先读 `README.md` 与本条宪法。
2. 新资料进入后，**先生成消化笔记，绝不跳过直接写成品**。
3. 消化笔记不答"对我有什么用" = 未完成。
4. 输出成果必须附**输出回流卡**。
5. 默认**先文章化，再视频化**。
6. **绝不替主人删除原始资料**——溯源依据永远留在 `00-原始资料/`。
7. 关联只连**真实存在且主人认可**的笔记/项目；禁止臆测，禁止链不存在或不活跃条目（历史踩坑：曾误链 MKtech / LabEqManager / sparkcanvas）。
8. 不擅自改动只读目录（`00` 删除、`02` 内容）除非主人明确指令。

## 7. 完成后回执

请告诉我：
- 处理了哪份原始资料；
- 生成了哪篇消化笔记 / 哪个输出成果 / 是否沉淀了方法；
- 回流卡里新产生了什么观点或新问题；
- 本次违反了或触发了哪条边界（如有）。

---

*本宪法由 AI 管理员遵循。每逢重大调整需与肖聪确认；随知识体系发展定期回顾更新。*
