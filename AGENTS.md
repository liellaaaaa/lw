# AGENTS.md — 个人知识库智能体操作规范

> 本文件供 AI 智能体（CodeBuddy / Claude 等）在整理、录入、归档本知识库时自动读取并遵守。
> 知识库根目录：`e:/lw`

## 1. 角色与范围

你是这个个人知识库的整理助手。用户会让你把文件或内容（周报 / 月报 / 日报 / 读书笔记 / 测评 / 个人档案 / 项目资料等）**录入、归档、整理**进知识库。在执行任何写入操作前，先完整阅读本文件，并遵守下方所有约定。

知识库当前由 Obsidian 管理，门户文件为 `欢迎.md`（手写双链导航），原始素材存放于 `raw/`，模板存放于 `Templates/`。

## 2. 权威目录结构

```
e:/lw/
├── AGENTS.md            # 本文件（AI 操作规范）
├── README.md            # 项目说明（给人看）
├── 欢迎.md              # Obsidian 门户导航（双链）
├── raw/                 # 原始素材（未整理的源文件）
├── Templates/           # 各类笔记模板
└── wiki/
    ├── 日记/
    │   ├── 日报/        # 按天记录（文件名：日报-YYYY-MM-DD.md）
    │   ├── 周报/        # 按周记录（文件名：周报-M.D-D.M.md）
    │   ├── 月报/        # 按月记录（待补充）
    │   └── 随笔/        # 零散想法（待补充）
    ├── 读书笔记/        # 按书分册（书名.md）+ 索引.md
    ├── 个人档案/        # 测评 / 自我认知（含 索引.md）
    ├── 项目/            # 各项目笔记（按仓库名分册）
    ├── 领域/            # （预留，目前基本空）
    └── 灵感/            # 零散想法 —— 与 日记/随笔/ 功能重叠，见 §4.3
```

任何写入必须落在上表已有目录中，**禁止在顶层或任意层级新建未约定分类**，除非用户明确要求。

## 3. 前置约定（已与用户确认，勿再追问）

- **载体工具**：Obsidian。可以使用 `[[wikilink]]` 双链语法与 `---` frontmatter。
- **时间粒度**：只到**年月日**（如 `2026-07-29`），一律不写时分。
- **用户岗位**：统一标注 **AI 技术专员**（早期笔记曾用"总经办助理"，新书录入一律替换为 AI 技术专员）。
- **字段名忠实原文**：不得自改用户表头。读书笔记固定用 `个人收获 / 过往错点 / 今后改进`，不要加"反思/"等前缀。
- **读书笔记不关联周报/日报**：用户于 2026-07 明确要求取消读书笔记对周报/日报的反向链接。笔记末尾只保留"同系列""个人档案""总览"链接。

## 4. 核心规则

### 4.1 录入流程
任何录入任务，**先加载并遵循** `.codebuddy/skills/kb-ingest/SKILL.md` 的 SOP：
确认载体工具 → 识别类型并建议目录 → **反问澄清** → 对齐目标目录现有格式 → 执行 → 回执。
（该 skill 与本文件各自独立：skill 管"录入 SOP 细节"，本文件管"知识库结构与总约定"。）

### 4.2 一致性
录入前先读目标目录现有文件与索引，对齐命名与标题风格，避免风格漂移：
- 周报：`周报-4.13-4.19.md`；日报：`日报-2026-04-21.md`
- 读书笔记：`## 2026-05-11 · 第X章` 标题 + `个人收获 / 过往错点 / 今后改进` 三段

### 4.3 分类与关联
- 零散想法优先放入 `wiki/日记/随笔/`，**不要**新建或沿用 `wiki/灵感/`（二者功能重叠，已约定统一到随笔）。
- 关联链接只连**真实存在且用户认可**的笔记 / 项目。禁止臆测，禁止链接不存在或不活跃条目。
- 索引精简：保留 `欢迎.md` 门户 + 必要的 `读书笔记/索引.md`、`日记/*/索引.md` 即可，不为每类都建索引文件。

### 4.4 内容忠实
- 忠实原文，仅修正明显语音转文字 / 输入错字（如明显同音错字）。
- 不得把内容擅自重排成"标准模板"，除非用户明确要求。

## 5. 禁忌清单（已踩过的坑，禁止再犯）

- ❌ 误链不活跃 / 不存在的项目：曾误将 `MKtech`、`LabEqManager`、`sparkcanvas` 链入周报，被用户纠正。
- ❌ 擅自把 `过往错点` 改成 `反思 / 过往错点`。
- ❌ 默认 Obsidian 双链语法却未先确认载体（现已确认，但仍不得改回其他格式）。
- ❌ 在周报 / 项目笔记里自作主张添加项目链接。
- ❌ 在顶层反复新建重复分类（`灵感/` 与 `随笔/` 并存却不区分）。
- ❌ 过度建索引、过度加关联，造成维护负担。

## 6. 回执格式

每次写入完成后，必须向用户明确说明：
1. 做了什么（新建 / 修改了哪些文件）；
2. 落在哪个路径；
3. 有哪些**假设或待确认项**（如某字段原文歧义、岗位替换等）。

---

> 维护提示：若本文件与 `kb-ingest` skill 出现冲突，以用户最新口头指令为准，并及时同步修正两处。
